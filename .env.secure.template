# 🔐 SECURITY AUDIT FINDINGS - ACTION REQUIRED ⚠️
#
# TWO CRITICAL SECURITY ISSUES FOUND IN YOUR PROJECT:
#
# 1. .env file contains LIVE API KEYS (exposed on disk)
# 2. serviceAccountKey.json contains PRIVATE credentials
#
# ⚠️ NEVER commit these files to git!
# ✅ They are in .gitignore (good) but still vulnerable locally
#
# ============================================================================
# IMMEDIATE ACTIONS REQUIRED:
# ============================================================================
#
# 1️⃣  REVOKE CURRENT KEYS (Do this now!)
#     - Go to: https://console.firebase.google.com/
#     - Go to: https://console.cloud.google.com/apis/credentials
#     - Delete the exposed API keys
#     - Generate NEW keys to replace them
#
# 2️⃣  REGENERATE SERVICE ACCOUNT KEY
#     - Go to Firebase Console → Project Settings → Service Accounts
#     - Delete the current key (610c4f73f272accaaf5afa8f79d1c83e338990fb)
#     - Generate a NEW key
#     - Never commit it to git!
#
# 3️⃣  REGENERATE GEMINI API KEY
#     - Go to: https://ai.google.dev/
#     - Regenerate your API key
#     - The exposed key: AIzaSyBRDQUZAhSH5c6k-jlgMTsi5J7yHwF0rqk
#
# 4️⃣  UPDATE THIS FILE with NEW KEYS
#     - Replace all values below with new generated keys
#     - Keep this file PRIVATE on your machine
#     - Share only placeholder values with team
#
# 5️⃣  SHARE ONLY THE TEMPLATE
#     - Share .env.example (with placeholder values) with team
#     - Each dev creates their own .env locally
#
# ============================================================================
# 📋 ENVIRONMENT VARIABLES TEMPLATE (Add your NEW keys here)
# ============================================================================

# Firebase Configuration (Get from Firebase Console → Project Settings)
VITE_FIREBASE_API_KEY=YOUR_NEW_FIREBASE_API_KEY_HERE
VITE_FIREBASE_AUTH_DOMAIN=YOUR_PROJECT.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=YOUR_PROJECT_ID_HERE
VITE_FIREBASE_STORAGE_BUCKET=YOUR_PROJECT.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=YOUR_MESSAGING_SENDER_ID_HERE
VITE_FIREBASE_APP_ID=YOUR_APP_ID_HERE

# Gemini API Key (Get from: https://ai.google.dev/)
VITE_GEMINI_API_KEY=YOUR_NEW_GEMINI_API_KEY_HERE

# Application Configuration
VITE_APP_NAME=AlgoView
VITE_APP_URL=http://localhost:5173

# ============================================================================
# 🔒 SECURITY CHECKLIST
# ============================================================================
#
# ✅ .env file is in .gitignore
# ✅ .env file is NOT committed to git
# ✅ Only .env.example is shared with team (with placeholder values)
# ✅ Each developer has their own local .env with THEIR OWN API keys
# ✅ Never use the same key across environments (dev, staging, prod)
# ✅ Rotate keys regularly (at least quarterly)
# ✅ serviceAccountKey.json is in .gitignore
# ✅ Monitor Firebase Console for suspicious activity after key rotation
#
# ============================================================================
